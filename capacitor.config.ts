import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'auth.yt',
  appName: 'auth-yt',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
