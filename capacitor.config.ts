import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ggi',
  appName: 'ggi',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
